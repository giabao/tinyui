import openfl.display.Sprite;
import openfl.text.TextField;

@:build(TinyUI.build('ui/14-for-loop.xml'))
class UI14ForLoop extends Sprite {
    public function new() {
        super();
        initUI();
    }
}
